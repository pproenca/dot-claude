# dot-claude secrets from 1Password (single call for speed)
# Safe to commit - .env.tpl contains only references, not actual secrets

op inject -i .env.tpl -o .env.local --force 2>/dev/null && dotenv .env.local

echo "Loaded dot-claude secrets from 1Password"
