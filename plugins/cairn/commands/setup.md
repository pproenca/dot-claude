---
description: Bootstrap the harness in this repo (once) — detect layout, write config.
---
Engage harness-setup: detect the layer layout, propose and write
boundary.config.json, the namespaced .harness/<skill>/ mirror, AGENTS.md, and the
canonical docs (.harness/CONSTITUTION.md + .harness/HARNESS.md via docs_init.py).
