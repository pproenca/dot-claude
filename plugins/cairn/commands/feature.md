---
description: Build something new — gated plan, skeleton review, then build.
argument-hint: [what to build]
---
Engage feature-workflow for: $ARGUMENTS
New behavior → STAGE 0 plan gate (boundary decomposition, shelf-check) → STAGE 0.5
skeleton review (confirm the spec) → build → verify → record.
