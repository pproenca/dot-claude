---
title: {{RULE_TITLE}}
tags: {{CATEGORY_PREFIX}}, {{CONCEPT}}, {{CONCEPT}}
# Optional — performance skills only. Omit both for correctness/idiom/API rules.
# impact: {{CRITICAL|HIGH|MEDIUM|LOW}}
# impactDescription: {{honest consequence, e.g. "2-10× improvement" or "prevents stale reads"}}
---

## {{RULE_TITLE}}

{{WHY — 1-3 sentences. Name the wrong default (what the model/engineer does here
without this rule) and its concrete consequence. This is the highest-signal part:
the model generalizes from the reason, not from the instruction.}}

```{{LANGUAGE}}
{{The canonical way. Production-realistic names — never foo/bar. This single
example is usually all that's needed.}}
```

Reference: [{{SOURCE_TITLE}}]({{SOURCE_URL}})

<!-- ───────────────────────────────────────────────────────────────────────
USE A FOIL ONLY WHEN THE WRONG WAY IS A REAL, COMMON TRAP.
A strawman "incorrect" example nobody would write is worse than none. When the
contrast genuinely teaches, replace the single example above with a minimal-diff
pair (same names, only the key line changes):

  **Incorrect ({{what's wrong}}):**
  fenced code block

  **Correct ({{what's right}}):**
  fenced code block

OPTIONAL SECTIONS (include only when they carry weight):
  **When NOT to use this pattern:** — real exceptions
  **Alternative ({{context}}):** — a second valid approach
─────────────────────────────────────────────────────────────────────── -->
