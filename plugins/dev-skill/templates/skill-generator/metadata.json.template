{
  "version": "{{VERSION}}",
  "organization": "{{ORGANIZATION_NAME}}",
  "date": "{{MONTH}} {{YEAR}}",
  "abstract": "Comprehensive performance optimization guide for {{TECHNOLOGY_NAME}} applications, designed for AI agents and LLMs. Contains {{RULE_COUNT}}+ rules across {{CATEGORY_COUNT}} categories, prioritized by impact from critical ({{CRITICAL_CATEGORIES}}) to incremental ({{LOW_CATEGORIES}}). Each rule includes detailed explanations, real-world examples comparing incorrect vs. correct implementations, and specific impact metrics to guide automated refactoring and code generation.",
  "references": [
    {{FOR_EACH REF in ALL_REFERENCES}}
    "{{REF.url}}"{{COMMA_IF_NOT_LAST}}
    {{END_FOR_EACH}}
  ]
}
