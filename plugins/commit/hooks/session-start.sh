#!/usr/bin/env bash
# SessionStart hook for commit-organizer plugin
# Context injection removed - guidelines available via skill lookup when needed
# This reduces SessionStart context pollution per Claude Code best practices

echo '{}'
exit 0
