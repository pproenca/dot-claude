[project]
name = "dot-claude"
version = "1.0.0"
description = "Personal Claude Code plugins marketplace"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
]

[tool.uv]
default-groups = ["dev"]
